#include <iostream>
#include <vector>

using namespace std;

struct change {
    unsigned short int type;
    unsigned long int line_number;
    string line_data;
};

bool update(vector<change> changes);
{
    
}
